name: Release

on:
  workflow_call:
    inputs:
      tag:
        required: true
        type: string
      sha:
        required: true
        type: string
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  RELEASE_TAG: ${{ github.event_name == 'workflow_call' && inputs.tag || github.ref_name }}
  RELEASE_SHA: ${{ github.event_name == 'workflow_call' && inputs.sha || github.sha }}

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            arch: amd64
          - os: linux
            arch: arm64
          - os: darwin
            arch: amd64
          - os: darwin
            arch: arm64
          - os: windows
            arch: amd64
          - os: windows
            arch: arm64

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.RELEASE_SHA }}

      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true

      - name: Build release archive
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: '0'
        run: |
          set -euo pipefail
          TAG="${RELEASE_TAG}"
          OUT_DIR="dist/${GOOS}_${GOARCH}"
          mkdir -p "${OUT_DIR}/brain"
          BIN_NAME="brain"
          ARCHIVE_EXT="tar.gz"
          if [ "${GOOS}" = "windows" ]; then
            BIN_NAME="brain.exe"
            ARCHIVE_EXT="zip"
          fi
          go build \
            -ldflags="-s -w -X 'brain/internal/buildinfo.Version=${TAG}' -X 'brain/internal/buildinfo.Commit=${RELEASE_SHA}' -X 'brain/internal/buildinfo.Date=$(date -u +%Y-%m-%dT%H:%M:%SZ)'" \
            -o "${OUT_DIR}/brain/${BIN_NAME}" .
          if [ "${GOOS}" = "windows" ]; then
            (cd "${OUT_DIR}/brain" && zip -q "../../../dist/brain_${TAG}_${GOOS}_${GOARCH}.${ARCHIVE_EXT}" "${BIN_NAME}")
          else
            tar -C "${OUT_DIR}/brain" -czf "dist/brain_${TAG}_${GOOS}_${GOARCH}.${ARCHIVE_EXT}" "${BIN_NAME}"
          fi

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.os }}-${{ matrix.arch }}
          path: dist/brain_${{ env.RELEASE_TAG }}_${{ matrix.os }}_${{ matrix.arch }}.*

  release:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: release-*
          merge-multiple: true

      - name: Create checksums
        run: |
          set -euo pipefail
          cd dist
          sha256sum brain_${RELEASE_TAG}_*.* > "brain_${RELEASE_TAG}_checksums.txt"

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          target_commitish: ${{ env.RELEASE_SHA }}
          generate_release_notes: true
          files: |
            dist/brain_${{ env.RELEASE_TAG }}_*.tar.gz
            dist/brain_${{ env.RELEASE_TAG }}_*.zip
            dist/brain_${{ env.RELEASE_TAG }}_checksums.txt
