# Brain Wrapper For AI Agents

This wrapper assumes `brain` is installed and the local vault is the durable source of truth.

- Prefer note-centric actions over freeform scratchpads.
- Keep generated context packages in markdown files inside the vault.
- Use `brain content` commands when turning notes into outlines or publication state.

