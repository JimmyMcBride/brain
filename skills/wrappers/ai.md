# Brain Wrapper For AI Agents

Use `brain` as the repo-local contract for project memory and workflow.

- Read repo context before inventing parallel memory.
- Prefer `brain read`, `brain edit`, `brain find`, and `brain search`.
- Use `brain plan` and `brain brainstorm` for Brain-owned planning or ideation.
