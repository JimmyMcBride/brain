# Brain Wrapper For Codex

When a repo includes Brain context, treat `brain` as the source of truth for durable project memory.

- Read the root contract first.
- Prefer `brain search` and `brain find` before broad repo scanning when project memory matters.
- Use `brain edit`, `brain history`, and `brain undo` for managed note updates.
